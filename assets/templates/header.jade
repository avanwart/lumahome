header.top-bar
  .top-bar__container
    a.top-bar__logo(href="<?= esc_url(home_url('/')); ?>")
      include ../images/svg/logo-luma.svg
    a.top-bar__menu-button(href="#")
      include ../images/svg/menu-hamburger.svg
    nav.top-bar__nav
      - if (has_nav_menu('primary_navigation')):
      - wp_nav_menu(['theme_location' => 'primary_navigation', 'menu_class' => 'top-bar__list nav-primary'])
      - endif;